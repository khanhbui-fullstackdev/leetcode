list1 = [1,2,4], list2 = [1,3,4]

// To solve this problem, we need to learn how to add 2 list1 & list2 to output

output:=&ListNode{Val:0, Next:nil}
worker:=output

// Loop through list1
for list1!=nil (node1!=nil) ✅{
	newNode:= &ListNode{Val:node1.Val, Next:nil}
	worker.Next = newNode // {Val:1, Next:nil}
	worker = worker.Next // di chuyển lên toa tiếp theo {Val:0,Next:nil}
	
	list1=list1.Next // node2
}

for list1!=nil (node2!=nil) ✅{
	newNode:= &ListNode{Val:node2.Val, Next:nil}
	worker.Next = newNode // {Val:2, Next:nil}
	worker = worker.Next // di chuyển lên toa tiếp theo {Val:0,Next:nil}
	
	list1=list1.Next // node4
}

for list1!=nil (node4!=nil) ✅{
	newNode:= &ListNode{Val:node4.Val, Next:nil}
	worker.Next = newNode // {Val:2, Next:nil}
	worker = worker.Next // di chuyển lên toa tiếp theo {Val:0,Next:nil}
	
	list1=list1.Next // nil
}

// worker.Next.PrintAllListNode() // [1,2,4]

// After knowing how to add list1 into output, then we can start to solve