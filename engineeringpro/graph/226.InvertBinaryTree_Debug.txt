     2         ->             2
  1     3                  3     1
2     5                      5     2 

// stack = [invertTree(root2)]
{
	if root2 == nil ❌
	if root2.Left == nil && root2.Right == nil ❌
	
	leftNode:= root2.Left = node1
	rightNode:=root2.Right = node3
	
	// swap
	root2.Left = rightNode = node3
	root2.Right = leftNode = node1
	
	// recurse to swap child node
	_:= invertTree(leftNode) = invertTree(node1)
}

// stack = [invertTree(node1),invertTree(root2)]
{
	if node1 == nil ❌
	if node1.Left == nil && node1.Right == nil ❌
	
	leftNode:= node1.Left = node2
	rightNode:=node1.Right = nil
	
	// swap
	node1.Left = rightNode = nil
	node1.Right = leftNode = node2
	
	// recurse to swap child node
	_:= invertTree(leftNode) = invertTree(node2)
}

// stack = [invertTree(node2),invertTree(node1),invertTree(root2)]
{
	if node2 == nil ❌
	if node2.Left == nil && node2.Right == nil {
		return node2
	}	
}

// stack = [invertTree(node1),invertTree(root2)]
{
	if node1 == nil ❌
	if node1.Left == nil && node1.Right == nil ❌
	
	leftNode:= node1.Left = node2
	rightNode:=node1.Right = nil
	
	// swap
	node1.Left = rightNode = nil
	node1.Right = leftNode = node2
	
	// recurse to swap child node
	_:= return node2
	_:= invertTree(rightNode) = invertTree(nil) return nil
	
	return node1
}

// stack = [invertTree(root2)]
{
	if root2 == nil ❌
	if root2.Left == nil && root2.Right == nil ❌
	
	leftNode:= root2.Left = node1
	rightNode:=root2.Right = node3
	
	// swap
	root2.Left = rightNode = node3
	root2.Right = leftNode = node1
	
	// recurse to swap child node
	_:= return node1
	_:= invertTree(rightNode) = invertTree(node3)
}

// stack = [invertTree(node3),invertTree(root2)]
{
	if node3 == nil ❌
	if node3.Left == nil && node3.Right == nil ❌
	
	leftNode:= node3.Left = node5
	rightNode:=node3.Right = nil
	
	// swap
	node3.Left = rightNode = nil
	node3.Right = leftNode = node5
	
	// recurse to swap child node
	_:=invertTree(leftNode) = invertTree(node5)
}

// stack = [invertTree(node5),invertTree(node3),invertTree(root2)]
{
	if node5 == nil ❌
	if node5.Left == nil && node5.Right == nil {
		return node5
	}
}

// stack = [invertTree(node3),invertTree(root2)]
{
	if node3 == nil ❌
	if node3.Left == nil && node3.Right == nil ❌
	
	leftNode:= node3.Left = node5
	rightNode:=node3.Right = nil
	
	// swap
	node3.Left = rightNode = nil
	node3.Right = leftNode = node5
	
	// recurse to swap child node
	_:= return node5
	_:=invertTree(rightNode) = invertTree(nil) // return nil
	
	return node3
}

// stack = [invertTree(root2)]
{
	if root2 == nil ❌
	if root2.Left == nil && root2.Right == nil ❌
	
	leftNode:= root2.Left = node1
	rightNode:=root2.Right = node3
	
	// swap
	root2.Left = rightNode = node3
	root2.Right = leftNode = node1
	
	// recurse to swap child node
	_:= return node1
	_:= return node3
	
	return root
}